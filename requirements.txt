# Core UI
streamlit>=1.32.0
streamlit-mic-recorder>=0.0.4

# Speech-to-Text (local Whisper model — no API key needed)
openai-whisper>=20231117

# Audio format handling (requires ffmpeg installed on your system)
pydub>=0.25.1

# Local LLM via Ollama
ollama>=0.1.8
